[data]
# Google Drive folder ID with FaceScrub zip files (subfolder)
gdrive_folder_id = "1xaggTEK0jeSVX9bgGIefq0-FeR8Eq8PK"

# Google Drive folder ID for ChromaDB backup (chromadb.zip)
gdrive_chromadb_folder_id = "1CyrpU9Axgip8ZmXlRu4z-2aknnSvyan6"

# Random seed for all splits — change to reproduce a different partition
seed = 44

# Number of people to enroll (must be >= 80 per project spec)
n_enrolled = 100

# Split ratios for the *remaining* people after enrollment (must sum to 1.0)
train_ratio = 0.70
val_ratio   = 0.20
test_ratio  = 0.10

[paths]
raw_dir           = "data/raw"
train_dir         = "data/train"
val_dir           = "data/val"
test_dir          = "data/test"
enrolled_dir      = "data/enrolled"        # images used to BUILD embeddings (70%)
enrolled_test_dir = "data/enrolled_test"   # held-out images for testing     (30%)
db_dir            = "embeddings/chromadb"

[enrolled_split]
# Fraction of each person's images held out for testing (Task 1, 3, 4, 7).
# Per project spec: test images must be different from enrollment images.
test_ratio = 0.30
